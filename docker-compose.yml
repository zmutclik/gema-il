services:
  gema-il:
    build: .
    container_name: gema-il
    restart: unless-stopped
    ports:
      - "8031:8031"
    volumes:
      - .:/app
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
